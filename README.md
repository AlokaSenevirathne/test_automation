# IT3040 – Assignment 1: Test Automation

## 📌 Project Overview
This project automates the testing of a **Singlish to Sinhala translation system** using **Playwright**.  
It reads test cases from an Excel file, inputs them into the web application, and records the results automatically.

---

## ⚙️ Setup Instructions

### 1. Install Python
- Install **Python 3.11 or above**
- Make sure Python is added to PATH

### 2. Install Dependencies
Run the following command:

```bash
pip install playwright openpyxl